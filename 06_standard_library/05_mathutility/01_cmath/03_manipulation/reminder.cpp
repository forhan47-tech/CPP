#include <iostream>
#include <cmath>
using namespace std;

int main() {
    double a = 10.5, b = 3.0;
    cout << "fmod(a, b) = " << fmod(a, b) << endl;
    cout << "remainder(a, b) = " << remainder(a, b) << endl;
    return 0;
}
