#include <iostream>
#include <cmath>
using namespace std;

int main() {
    double x = 0.5;
    cout << "asin(x) = " << asin(x) << endl;
    cout << "acos(x) = " << acos(x) << endl;
    cout << "atan(x) = " << atan(x) << endl;
    return 0;
}
