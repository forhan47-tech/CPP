#include <iostream>
using namespace std;

int main() {
    bool a = true, b = false;
    
    cout << (a && b) << endl; // AND
    cout << (a || b) << endl; // OR
    cout << (!a) << endl;     // NOT
    return 0;
}
