#include <iostream>
using namespace std;

int main() {
    int age = 20;

    if (age >= 18) {
        if (age < 21) {
            cout << "Adult but not 21 yet" << endl;
        }
    }

    return 0;
}
