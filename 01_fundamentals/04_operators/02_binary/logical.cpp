#include <iostream>
using namespace std;

int main() {
    bool p = true, q = false;

    cout << "p AND q: " << (p && q) << endl;
    cout << "p OR q: " << (p || q) << endl;
    cout << "NOT p: " << (!p) << endl;

    return 0;
}
