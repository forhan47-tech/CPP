#include <iostream>
using namespace std;

int main() {
    signed int s = -40;  // allows negatives
    unsigned int u = 40; // Only non-negative

    cout << "Signed: " << s << endl;
    cout << "Unsigned: " << u << endl;
    return 0;
}